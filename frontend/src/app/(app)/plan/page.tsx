// Unified "Plan a Trip" flow — one entry point that scales from a single
// traveler to a whole group. The implementation lives in the (reframed) group
// planning page; this route is the canonical, user-facing home for it.
export { default } from '../group-planning/new/page';
