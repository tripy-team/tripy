# Legacy root requirements.txt - DEPRECATED
# This file is kept for compatibility but should not be used for new deployments
# Use backend/requirements.txt instead for backend dependencies

# Note: Some packages here (futures, typing, pathlib2, scandir) are Python 2 compatibility packages
# and should not be installed for Python 3.9+ projects

# If App Runner or other services need to use this file, update it to remove:
# - futures (not needed for Python 3)
# - typing (built into Python 3.5+)
# - pathlib2 (pathlib is built into Python 3.4+)
# - scandir (built into Python 3.5+ as os.scandir)
# - singledispatch (backport for Python 3.3, not needed for 3.4+)
