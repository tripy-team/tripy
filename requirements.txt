# --- web service ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
python-dotenv==1.0.1
boto3==1.35.23
openai

# --- data sources / scraping ---
requests==2.32.3
beautifulsoup4==4.12.3
serpapi==0.1.5

# --- cache (prefer redis client; see Step 3) ---
redis==5.0.8

# --- optional / experimental (comment these in only if you use them) ---
pulp==2.7.0            # ILP demos (flightGraph, planTrip)
# amadeus==8.0.0         # only if you wire Amadeus SDK in experimental
