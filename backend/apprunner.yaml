version: 1.0
runtime: python311
build:
  commands:
    build:
      - echo "Using 3.11; deps install in pre-run"
run:
  pre-run:
    - pip3 install --upgrade pip
    - pip3 install -r requirements.txt
  command: uvicorn app:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
